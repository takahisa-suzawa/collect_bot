<%= form_with(model: infomation, local: true) do |form| %>
  <% if infomation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(infomation.errors.count, "error") %> prohibited this infomation from being saved:</h2>

      <ul>
      <% infomation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_area :text, id: :infomation_text %>
  </div>

  <div class="field">
    <%= form.label :report_id %>
    <%= form.text_field :report_id, id: :infomation_report_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
