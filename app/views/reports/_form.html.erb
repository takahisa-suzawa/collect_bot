<%= form_with(model: report, local: true) do |form| %>
  <% if report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(report.errors.count, "error") %> prohibited this report from being saved:</h2>

      <ul>
      <% report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :begin_posted_date %>
    <%= form.datetime_select :begin_posted_date, id: :report_begin_posted_date %>
  </div>

  <div class="field">
    <%= form.label :end_posted_date %>
    <%= form.datetime_select :end_posted_date, id: :report_end_posted_date %>
  </div>

  <div class="field">
    <%= form.label :infomation %>
    <%= form.text_area :infomation, id: :report_infomation %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
